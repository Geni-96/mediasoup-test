# IndexedCP Configuration (Optional)
# If not configured, the system will continue to work normally without IndexedCP uploads

# IndexedCP service URL - standardized to port 3000 for HTTP API calls
INDEXEDCP_SERVER=http://localhost:3000

# Note: API key handling is now managed by the standalone IndexedCP service
# Remove INDEXEDCP_API_KEY as it's no longer needed for HTTP API calls

# IndexedCP timeout in milliseconds
INDEXEDCP_TIMEOUT=30000
