# Frontend IndexedCP Configuration (Optional)
# If not configured, the system will continue to work normally without IndexedCP uploads

# IndexedCP server URL (same as backend for consistency)
REACT_APP_INDEXEDCP_SERVER=http://localhost:8080

# IndexedCP API key (will prompt if not provided)
REACT_APP_INDEXEDCP_API_KEY=your-api-key-here

# IndexedCP timeout in milliseconds
REACT_APP_INDEXEDCP_TIMEOUT=30000
